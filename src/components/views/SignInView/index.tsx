// ! Delete this comment or delete component if its doesn't use
import { FC } from 'react';

import { SignInFormSmart } from './SignInFormSmart';

export const SignInView: FC = () => {
  return <SignInFormSmart />;
};
