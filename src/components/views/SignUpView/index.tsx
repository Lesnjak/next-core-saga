// ! Delete this comment or delete component if its doesn't use
import { FC } from 'react';

import { SignUpFormSmart } from './SignUpFormSmart';

export const SignUpView: FC = () => {
  return <SignUpFormSmart />;
};
