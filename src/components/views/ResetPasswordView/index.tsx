// ! Delete this comment or delete component if its doesn't use
import { FC } from 'react';

import { ResetPasswordFormSmart } from './ResetPasswordFormSmart';

export const ResetPasswordView: FC = () => {
  return <ResetPasswordFormSmart />;
};
