// ! Delete this comment or delete component if its doesn't use
import { FC } from 'react';

import styles from './styles.module.scss';

export const TableBody: FC = () => {
  return <div className={styles.tableBodyWrapper}>Table body</div>;
};
